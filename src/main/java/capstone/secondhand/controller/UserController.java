package capstone.secondhand.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("/users")
public class UserController {
/*

    // 유저 프로필
    @GetMapping("/{userId}/profile")

    // 유저 프로필 업데이트
    @GetMapping("/{userId}/profile/update")

    @PostMapping("/{userId}/profile/update")

    // 유저 삭제
    @PostMapping("/{userId}/delete")

    // 유저 옷장
    @GetMapping("/{userId}/wardrobe")

    // 유저 옷장 옷 등록
    @GetMapping("/{userId}/wardrobe/new")

    @PostMapping("/{userId}/wardrobe/new")

    // 유저 활동(전체)
    @GetMapping("/{userId}/history")

    // 유저의 찜목록
    @GetMapping("/{userId}/history/favorites")

    // 유저 찜목록 삭제
    @PostMapping("/{userId}/history/favorites/delete")

    // 유저의 판매목록
    @GetMapping("/{userId}/history/sell")

    // 유저의 구매목록
    @GetMapping("/{userId}/history/buy")
*/

}
